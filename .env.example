# Ollama (padrão: localhost)
# OLLAMA_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.2:3b

# Piper — modo local (padrão)
# PIPER_BIN=bin/piper/piper
# PIPER_MODEL=models/pt_BR-faber-medium.onnx

# Piper — modo Docker (define PIPER_URL para usar o serviço HTTP)
# PIPER_URL=http://localhost:5000

# Diretório de saída (padrão: ./output)
# OUTPUT_DIR=output
